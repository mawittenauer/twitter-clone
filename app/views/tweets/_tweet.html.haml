.well
  .row
    .col-xs-2
      = image_tag(tweet.user.avatar_url, class: "tweet-profile-picture")
    .col-sx-10
      %h4= link_to "#{tweet.tag}", tweet.user
      %p= tweet.body
      = link_to "Reply", reply_user_path(tweet.user), class: "pull-right reply"
        